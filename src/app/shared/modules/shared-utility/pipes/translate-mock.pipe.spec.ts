import { TranslateMockPipe } from './translate-mock.pipe';

describe('TranslateMockPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateMockPipe();
    expect(pipe).toBeTruthy();
  });
});
