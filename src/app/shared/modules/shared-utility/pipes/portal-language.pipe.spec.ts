import { PortalLanguagePipe } from './portal-language.pipe';

describe('PortalLanguagePipe', () => {
  it('create an instance', () => {
    const pipe = new PortalLanguagePipe();
    expect(pipe).toBeTruthy();
  });
});
